apiVersion: v1
kind: ConfigMap
metadata:
  name: test-config
  namespace: drift-test
  labels:
    app: test-app
data:
  app.conf: |
    server {
      listen 80;
      server_name localhost;
    }
  database.url: "postgres://localhost:5432/testdb"
  cache.enabled: "true"
